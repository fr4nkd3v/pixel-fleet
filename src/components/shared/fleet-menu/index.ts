import { FleetMenuPlayer } from "./fleet-menu-player";
import { FleetMenuOpponent } from "./fleet-menu-opponent";

export const FleetMenu = {
  player: FleetMenuPlayer,
  opponent: FleetMenuOpponent,
};
